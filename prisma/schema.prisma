generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "mongodb"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_DATABASE_URL")
}

model Accounts {
  id        String  @id @map("_id")
  isPrimary Boolean @default(false)
  ownerId   String

  @@index([ownerId])
}

model Servers {
  id              String   @id @map("_id")
  name            String
  imageUrl        String?
  groupId         String?
  inviteChannelId String?
  parentServerId  String?
  ownerId         String
  parentServer    Servers?  @relation("ParentChild", fields: [parentServerId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  childServers    Servers[] @relation("ParentChild")

  @@index([ownerId])
}
